# Закрытые пункты аудита — период 2026-04-17 → 2026-04-27

**Источник:** перенесено из `STATUS.md` при ре-аудите 2026-04-25.
**Назначение:** исторический архив — фиксирует, какие находки были закрыты, какие фиксы применены, какие альтернативы отвергнуты и почему. На активный workflow не влияет; `STATUS.md` теперь содержит только открытые пункты.

ID находок стабильны: на ту же находку в `STATUS.md` (если откроется регрессия или связанный пункт) ссылаемся тем же ID.

---

## Platform

### Critical

- [x] **P-C1** — `go.mod:3` объявляет `go 1.26.1`. ~~Релиз не существует~~ (закрыт 2026-04-17: ложная находка аудитора из-за training cutoff — Go 1.26 релизнут в феврале 2026, версия валидна, CI использует `go-version-file: go.mod` корректно).
- [x] **P-C2** — `internal/services/gateway/handlers.go:220` — NATS `RequestMsg` игнорирует `r.Context()` и фиксированный 5s таймаут (закрыт 2026-04-17: `RequestMsgWithContext` с `WithTimeout(r.Context(), cfg.HTTP.NATSRequestTimeout)`, добавлен `GATEWAY_NATS_REQUEST_TIMEOUT` env/default 5s, `http.Server.BaseContext = gw.RootContext()` в main — shutdown теперь отменяет все in-flight запросы; ошибки классифицируются в 499/504/503 через `natsRequestErrStatus`).
- [x] **P-C3** — `internal/services/gateway/handlers.go:367-376` — race NATS-коллбэк vs `conn.Close()` (закрыт 2026-04-17: `safeWrite` проверяет `ctx.Done()` под mu; `conn.Close()` вызывается под тем же mu через объединённый defer `cancel → Lock → Close` — последним в LIFO после `sub.Unsubscribe`; NATS-коллбэк и теперь содержит быстрый `ctx.Err()` exit до mu, чтобы при массовом shutdown не толпиться на lock).
- [x] **P-C4** — `internal/services/gateway/handlers.go:325-329` — при ошибке `rand.Read` для sessionID функция возвращается без отправки CloseMessage клиенту (закрыт 2026-04-17: генерация `sessionID` перенесена ДО `Upgrade`; при сбое CSPRNG отдаётся `http.Error 500` + `releaseConn()`, после `Upgrade` точка отказа исключена — handler работает в едином инварианте `conn != nil ⇒ rand отработал`).

### High

- [x] **P-H1** — `internal/services/gateway/ratelimit.go:77-84` — `evictOldest` выполняет O(N) линейный скан таблицы IP под `mu` (закрыт 2026-04-17: переход на настоящий LRU — `ipTable` = `map + container/list.List`, у `ipEntry` появился `elem *list.Element`. На hit `MoveToFront(elem)` — O(1). При переполнении удаляется `lst.Back()` — O(1) вместо O(N) сканирования. Cleanup тоже O(K) вместо O(N) — back-walk по списку до первой неэкспирированной записи. Self-amplification под DDoS с уникальных IP снят, время под `mu` константное независимо от `MaxIPs`).
- [x] **P-H2** — `cmd/gateway/main.go:64` — `log.Fatal()` вызывается внутри горутины (закрыт 2026-04-17: ошибка `ListenAndServe` направляется в buffered `serverErr`-канал; main через `select` ловит либо SIGINT/SIGTERM, либо server error, и в обоих случаях идёт по единому shutdown path — `close(stopCh)` → `server.Shutdown` → `natsClient.Drain`. Non-zero `exitCode` сохраняется и вызывается `os.Exit(1)` в самом конце для Nomad restart logic; `cancel()` shutdownCtx вызывается явно, так как defer не сработает после os.Exit).
- [x] **P-H3** — `internal/platform/nc/client.go:336-343` — `Drain` реализован busy-wait циклом (закрыт 2026-04-17: переход на event-driven — в `PlatformClient` добавлено поле `closed chan struct{}`, в `nats.ClosedHandler` (уже зарегистрированном) добавлен `close(closed)`. `Drain` теперь использует `select { case <-p.closed: ...; case <-time.After(timeout): ... }` — ноль задержки на детект завершения, ровно один источник истины о закрытии соединения. Hard-close по таймауту тоже триггерит ClosedHandler, но `Drain` уже ушёл с ошибкой; повторное закрытие канала не происходит, т.к. close выполняется только в одном месте — handler'е).
- [x] **P-H4** — `internal/middleware/xauth.go:87` — ответ собирается конкатенацией (закрыт 2026-04-17: переход на `json.Marshal(struct{Error string})` — корректный escape любых спецсимволов в `text`, защищён от injection при будущей передаче динамических значений; error от `json.Marshal` для `struct{string}` безошибочен и игнорируется явным `_`).
- [x] **P-H5** — `internal/middleware/xauth.go:64` — проверка `Exp` access-токена без clock-skew tolerance. Исходная формулировка аудита про «двойную проверку `exp`» неверна: `xauth.VerifyJWT` намеренно **не** проверяет `Exp` (jwt.go:45-47 — by design, чтобы middleware мог разделять «expired» → /refresh и «invalid» → login). Реальный риск — при дрейфе часов между узлами (NTP ±секунды) свежевыданный токен может оказаться «истёкшим». Закрыт 2026-04-17: в `internal/middleware/xauth.go` добавлена константа `accessTokenClockSkew = 60 * time.Second`; проверка превратилась в `time.Now().Unix() > c.Exp + int64(skew.Seconds())`. RFC 7519 §4.1.4 рекомендует «small leeway»; 60s — индустриальный дефолт, жёстче смысла нет (access-токены короткоживущие).
- [x] **P-H6** — `internal/services/gateway/handlers.go:120,125` (аудитор указал 111/116 — ошибся номерами строк, тело верное) — `w.Write()` в `/health` молчаливо игнорировал ошибку. Закрыт 2026-04-17: оба вызова обёрнуты в `_, _ = w.Write(...)`, добавлен комментарий-пометка о намеренном игнорировании (после `WriteHeader` восстановиться нельзя; tcp-разрыв клиента в healthcheck — норма). Debug-лог не добавляли — Nomad бьёт `/health` каждые несколько секунд, любой transient разрыв создал бы шум в логах.

### Medium

- [x] **P-M1** — `internal/services/gateway/handlers.go:160` — маршрут `/v1/{service}/ws` не проверяет `r.Method`. WS upgrade принимает POST/PUT. Fix: `if r.Method != http.MethodGet { http.Error(...) }`. **2026-04-17:** в `route()` перед вызовом `handleWS` добавлена проверка `r.Method != http.MethodGet → 405`. Отбивает до `wsConnGuard`/`rand.Read`/`Upgrade` (без неё `gorilla` всё равно вернул бы 405, но уже после резерва WS-slot и генерации SID).
- [x] **P-M2** — `internal/services/gateway/handlers.go:186-188` — `service` и `methodParts` идут в NATS subject без санитизации. Символы `*`, `>`, пробел — спецсимволы NATS, позволяют манипулировать маршрутизацией. Fix: валидация через regex `^[a-zA-Z0-9_-]+$` для каждого сегмента. **2026-04-17:** добавлена пакетная переменная `validSubjectToken = regexp.MustCompile(` + "`" + `^[A-Za-z0-9_-]+$` + "`" + `)`; в `route()` цикл по `parts[1:]` (валидирует и service, и methodParts; покрывает обе ветки — HTTP и WS). Старый `if service == ""` удалён — пустая строка не проходит regex.
- [x] **P-M3** — `internal/services/gateway/ratelimit.go:184-197` — `realIP` возвращает `X-Real-IP` как есть, без `net.ParseIP`. Скомпрометированный или misconfigured прокси может передать пустую строку или мусор → все запросы учитываются под одним ключом, rate limit обходится. Fix: `if ip := net.ParseIP(v); ip != nil { return ip.String() }`. **2026-04-17:** в `realIP` (фактически строки 211-228, аудитор ошибся в номере) добавлен `net.ParseIP` поверх `X-Real-IP`; невалидное значение тихо пропускается, fallback на `r.RemoteAddr`. Защищает LRU rate-limiter от мусор-DoS при ротации невалидных X-Real-IP. `parsed.String()` нормализует IPv6.
- [x] **P-M4** — `internal/platform/logger/logger.go:31` — `zerolog.SetGlobalLevel` — глобальный side-effect в фабрике. Каждый вызов `logger.New()` перезаписывает уровень для всех логгеров процесса. Fix: выставлять уровень через `logger.Level(level)` на конкретном экземпляре. **2026-04-17:** `zerolog.SetGlobalLevel` удалён, уровень ставится через `.Level(level)` на возвращаемом instance. zerolog как библиотека не меняется. Default `GlobalLevel` остаётся на `DebugLevel(0)` → instance.Level — эффективный фильтр (zerolog `should()` берёт max(instance, global)).
- [x] **P-M5** — `internal/services/gateway/handlers.go:419-426` — нет `conn.SetReadLimit(...)`. gorilla defaults (32KB) легко насыщаются большими фреймами. Fix: `conn.SetReadLimit(64 * 1024)` после Upgrade. **2026-04-17:** добавлена константа `wsReadLimit = 64 * 1024` рядом с `wsWriteDeadline`/`wsReadDeadline`/`wsPingInterval`; `conn.SetReadLimit(wsReadLimit)` сразу после Upgrade. Аудитор ошибся в numerics — gorilla default `0` (unlimited), не 32 КБ; угроза верна (memory-DoS на одно соединение), фикс закрывает.
- [x] **P-M6** — `utils/log.go:8-13` — глобальная `pkgLog` без защиты (ни atomic, ни mutex). Сейчас `SetLogger` вызывается строго до старта горутин, но гарантии нет. Fix: `atomic.Pointer[zerolog.Logger]`. **2026-04-18:** выбран DI-вариант (compile-time гарантии вместо runtime accepted-risk). `utils/log.go` удалён вместе с `pkgLog`/`SetLogger`/`Logger`. `GetEnv`, `Reply`, `ReplyError`, `ParseAllowedHosts`, `AllowedHostSet.Allows` принимают `log zerolog.Logger` первым параметром. `LoadConfig(log)` во всех 4 сервисах; `cmd/*/main.go` пробрасывают `log` без `SetLogger`. Мёртвый код `MustParseAllowedHosts`/`AllowedHostsFromEnv` удалён. Гонки данных по логгеру невозможны: компилятор не позволит передать «не тот» экземпляр.
- [x] **P-M7** — `internal/services/gateway/handlers.go:444-465` — connect-сообщение отправляется через `Publish` (fire-and-forget) без подтверждения. При отсутствии подписчика WS остаётся открытым, не работая. Fix: использовать `RequestMsg` для connect с 2s таймаутом, при отказе — WS close 503. **2026-04-18:** `PublishMsg` заменён на `RequestMsgWithContext`; добавлено поле `HTTPConfig.WSConnectTimeout` + env `GATEWAY_WS_CONNECT_TIMEOUT` (default `2s`). При ошибке логируется Warn для `nats.ErrNoResponders` (понятная диагностика «сервис не подписан») и Error для остальных (DeadlineExceeded и т.п.); клиенту отправляется WS Close `1011 service unavailable` (CloseInternalServerErr — `1013 Try Again Later` в gorilla не предопределён, выбран ближайший стандартный код), defer закрывает соединение. Демо `cmd/xws/main.go` обновлён: после `mgr.Open(sid)` вызывает `msg.Respond(nil)` — ack-контракт стал частью протокола WS connect.
- [x] **P-M8** — `internal/services/gateway/handlers.go:514-550` — shutdown-горутина запускается без `sync.WaitGroup`. При return из `handleWS` горутина может обращаться к уже закрытому `conn`. Fix: `errgroup.Group` или `sync.WaitGroup`. **2026-04-18:** добавлена `var wg sync.WaitGroup` рядом с `mu`; обе побочные горутины (Ping и shutdown) обёрнуты в `wg.Add(1)`/`defer wg.Done()`. Объединённый defer теперь идёт по схеме `cancel() → wg.Wait() → mu.Lock() → conn.Close()` — гарантирует, что ни одна горутина не висит после возврата из `handleWS`. Дополнительно: в shutdown-горутине `conn.SetReadDeadline(time.Now())` шёл вне `mu` (формальная race с `conn.Close`); добавлен helper `safeSetReadDeadline` (по аналогии с `safeWrite`) — Lock + ctx.Done check + SetReadDeadline. Ping-горутина без race-окна вне mu, но включена в WaitGroup для симметрии lifecycle. errgroup отвергнут как ненужная абстракция — у горутин нет ошибок к возврату.
- [x] **P-M9** — нет встроенного механизма health-probe для NATS-only сервисов. Текущий Nomad-restart срабатывает только на process exit (panic/OOM/SIGKILL); deadlock-в-handler (висящий SQL, blocking I/O, забытый mutex) не детектится: TCP/PING-PONG отвечают из отдельной горутины nats.go, NATS-сервер считает клиента online, Nomad без probe — alloc healthy. Чтобы поймать такой deadlock, probe обязан идти через тот же dispatcher, что и бизнес-handler'ы — иначе он отличается от healthy только при разрыве соединения. Fix: helper в `internal/platform/nc` (например `nc.RegisterHealth(svc, port)`) регистрирует через основной mux handler `api.v1.<svc>._health` (отвечает пустой строкой) и поднимает HTTP `/healthz` на `127.0.0.1:<port>` (порт через ENV); `/healthz` делает self-roundtrip `nc.Request("api.v1.<svc>._health", "", 1s)` → 200/503. Сервис добавляет один вызов в `main.go` + одну ENV-переменную. Шаблон .nomad-блока (`network { port "health" {...} }` + `service { check { type = "http", path = "/healthz" } }`) добавить в `prod.md` «Добавить новый сервис». Gateway не трогать — у него уже HTTP `/health` через `nc.Status()` (для Gateway этого достаточно, т.к. он сам HTTP-сервер и его deadlock виден по росту 5xx на ALB). Применение к xauth/xhttp/xws закрывает I-L7 побочным эффектом — паттерн станет встроенной частью платформы, новые сервисы получают health «из коробки». **2026-04-23:** добавлен `internal/platform/nc/health.go` — `PlatformClient.RegisterHealth(svc, addr) (*HealthEndpoint, error)` подписывается на уникальный `_health.<svc>.<nuid>` через основной `Conn.Subscribe` (тот же dispatcher, что у бизнес-подписок) и поднимает HTTP `/healthz` на `addr` с self-roundtrip `RequestWithContext` (timeout 1s). `nuid` повышен до direct dep в `go.mod`. Различаем ошибки probe: `ErrNoResponders` → Warn, `DeadlineExceeded` → Error (deadlock), `ErrConnectionClosed` → Debug (shutdown). `HealthEndpoint.Shutdown(ctx)` вызывается ДО `natsClient.Drain()` — каноничный «fail health first, then drain». Применено к `cmd/{xauth,xhttp,xws}/main.go`: требуемый `HEALTH_ADDR` (Fatal при пустом), регистрация сразу после `nc.NewClient`, shutdown в shutdown-секции. Nomad-джобы (`deployments/infra/nomad/{xauth,xhttp,xws}.nomad`) получили `network { port "health" {} }` + `service { check { type = "http", path = "/healthz" } }` + `HEALTH_ADDR = "127.0.0.1:${NOMAD_PORT_health}"`. Dev-HEREDOC в `deployments/envs/dev/start.sh` синхронизирован (см. feedback-memory про setup.sh-дрейф — дрейф не допущен с первой итерации). Шаблон «Добавить новый сервис» в `prod.md` обновлён: добавлены блоки `network`/`service`/`HEALTH_ADDR` + пример `main.go` с `RegisterHealth`/`Shutdown`. Gateway не тронут (как и запланировано). Dev-валидация: 3/3 сервиса отдают `/healthz → 200` на выделенных Nomad dynamic-port (29963/28406/24852), `Total Restarts = 0`. Побочно закрывает I-L7 (см. ниже).

### Low

- [x] **P-L1** — `internal/services/gateway/handlers.go:257-262` — `Dur("ms", time.Since(start))` пишет наносекунды под ключом `ms`. Fix: `Int64("ms", time.Since(start).Milliseconds())`. **2026-04-18:** реальная строка — `handlers.go:298` (аудитор промахнулся в номере). `Dur("ms", time.Since(start))` заменён на `Int64("ms", time.Since(start).Milliseconds())` — `zerolog.Dur` сериализует `time.Duration` в наносекундах по умолчанию (`DurationFieldUnit = time.Nanosecond`), и под ключом `"ms"` уходило число вида `123456789`; теперь — целое число миллисекунд. Глобальная альтернатива (`zerolog.DurationFieldUnit = time.Millisecond`) отвергнута: side-effect на всю библиотеку процесса, а в платформе нет других `Dur(...)` с зависимостью от текущего unit (grep подтвердил единственное использование). `go build ./... && go vet ./...` чисто.
- [x] **P-L2** — `internal/middleware/xauth.go:89-92` — ошибка `RespondMsg` молча игнорируется (комментарий "no logger available"). Если клиент не получил 401 — запрос утекает. Fix: передавать `zerolog.Logger` в `replyUnauthorized`. **2026-04-18:** в сигнатуру `replyUnauthorized` добавлен параметр `log zerolog.Logger` вторым; 3 callsite в `RequireAuth` обновлены с передачей `cfg.Log`. Сбой `msg.RespondMsg` теперь логируется как `Error` с subject и err — оператор видит, если клиент остался без ответа. Сами 401-ответы не логируются (нормальный user-facing кейс — токен протух, клиент пойдёт на `/refresh`); `RequireAuth` уже пишет `Warn` для невалидной подписи токена (`xauth.go:67`), missing/expired остаются молчаливыми по дизайну. Альтернатива через `utils.ReplyError` отвергнута: она логирует каждый 401 как `Error` (`utils/reply.go:40`) — даст лог-спам и ложные алерты на error rate. `go build ./... && go vet ./...` чисто.
- [x] **P-L3** — `internal/platform/nc/client.go:255` — `CreateKeyValue` error treated as "already exists" для всех ошибок. Auth/network failures маскируются. Fix: `errors.Is(err, jetstream.ErrBucketExists)` явно. **2026-04-18:** в `initKV` (`client.go:262-275`) добавлен явный `errors.Is(err, jetstream.ErrBucketExists)` guard перед fallback-веткой; импорт `errors` добавлен в шапку. Раньше любая ошибка от `CreateKeyValue` (auth-fail, network, потеря кворума, misconfig) тихо вела на `KeyValue(...)` lookup — он тоже падал, и оператор видел расплывчатое «бакет недоступен» вместо реальной причины. Теперь не-bucket-exists ошибки возвращаются обёрнутыми с понятным сообщением `nats: create KV-бакет %q: <root cause>`. Sentinel `ErrBucketExists` существует в текущей версии `nats.go/jetstream` (`bucket name already in use`) — KV CreateKeyValue маппит на него `ErrStreamNameAlreadyInUse` от сервера (стандартный `errors.Is` через равенство указателей корректно ловит). `go build ./... && go vet ./...` чисто.
- [x] **P-L4** — `utils/hosts.go:108-112` — `AllowedHostsFromEnv` дублирует логику, используемую в `LoadConfig`. Dead code или лишний entry-point. Fix: удалить. **2026-04-18:** уже закрыт фактически как побочный эффект P-M6 (DI-рефактор `utils/log.go`). `AllowedHostsFromEnv` и `MustParseAllowedHosts` удалены вместе с глобальным `pkgLog`/`SetLogger` — оба функции были обёртками, читавшими `os.Getenv("ALLOWED_HOSTS")` и пробрасывавшими в `pkgLog`; после перехода на явную передачу логгера через `LoadConfig(log)` их сигнатура без аргументов потеряла смысл, и они стали мёртвым кодом. Grep по репо подтверждает: упоминания остались только в `audit/STATUS.md` (исходный audit-текст + closure-note P-M6). Кодовых правок в рамках P-L4 не потребовалось.
- [x] **P-L5** — `internal/platform/nc/client.go:304-310` — silent KV replica downgrade на err_code=10005 «no suitable peers for placement». Логика появилась в коммите `179e173` (DEV-Docker: NOMAD hand tested on 12 nodes) без открытия audit-пункта: при невозможности разместить R-реплик KV-бакета число реплик автоматически снижается на 1, до удачного create или R=1. Цикл логирует `Warn`, но без метрик (G5 открыт) оператор не увидит, что сервис стартовал с R=1 на 3-нодовом кластере вместо ожидаемого R=3 — деградация консистентности молча принята. **2026-04-25:** в `initKV` сохраняется `requested := cfg.Replicas` (нормализуется к 1 минимум), после успешного `CreateKeyValue` итоговый лог становится `Error` при `requested - replicas >= 2` (с дополнительным полем `requested`), иначе остаётся `Info`. Один шаг вниз (`requested - replicas == 1`) считается приемлемым — типичный first-start race meta-cluster, при retry без рестарта подтягивается. Два и больше — реальный misconfig (например, 5-нодовый кластер ждёт R=3 а JetStream даёт R=1), оператор видит Error в логах сразу, до молчаливой работы с пониженной отказоустойчивостью. Метрика `nats_kv_replicas{bucket}` — правильное долгосрочное решение, но до закрытия G5 (метрик нет) Error-лог — единственный возможный сигнал. `go build ./... && go vet ./...` чисто.

---

## CI/CD + Infra

### Critical

- [x] **I-C1** — `.github/actions/deploy-nomad/action.yml:85` — секреты передаются в `nomad job run` как `-var KEY=VALUE` в командной строке (закрыт 2026-04-17: `-var` флаги заменены на `export NOMAD_VAR_<NAME>=...` внутри SSH-скрипта; Nomad нативно читает эти env-переменные, значения попадают в `/proc/<pid>/environ` (читаемо только владельцем процесса), а не в world-readable cmdline; `printf '%q'` сохранён для безопасной shell-экранировки при интерполяции).
- [x] **I-C2** — `.github/actions/deploy-nomad/action.yml:37` — `echo "$DEPLOY_SSH_KEY" > /tmp/deploy_key` создаёт файл с дефолтным umask до строки 38 `chmod 600` (закрыт 2026-04-17: переход на `mktemp` + `trap EXIT` — файл создаётся атомарно с режимом `0600` под уникальным именем, race-окно на правах исчезло, symlink-attack на фиксированный путь `/tmp/deploy_key` тоже невозможен; финальный `rm -f /tmp/deploy_key` удалён, cleanup теперь гарантирован на любом пути выхода через trap).
- [x] **I-C3** — `.github/workflows/setup.yml` — script injection через `${{ inputs.* }}`-интерполяцию в bash-блоках (закрыт 2026-04-17: переформулирован — оригинальная находка про секреты неактуальна, secrets уже шли через `env:`-секцию ещё с коммита `3557643`. Реальная проблема — `${{ inputs.platform_domain }}`, `${{ inputs.node_ip }}` в heredoc/scp/ssh-блоках: heredoc `<<EOF` без кавычек выполняет `$()` в подставленном значении → RCE на runner при враждебном input. Фикс: все `inputs` перенесены в `env:` секции своих шагов, в скриптах используются `${PLATFORM_DOMAIN}`, `${NODE_IP}`, `${REPO_URL}`).
- [x] **I-C4** — `deployments/infra/nomad/{gateway,xauth,xhttp,xws}.nomad` — `artifact { checksum = var.CHECKSUM }` использует невалидный синтаксис: per Nomad docs `checksum` живёт внутри `options { checksum = "..." }`, не как direct attribute блока `artifact`. Найдено 2026-04-18 при `nomad job validate` в рамках I-M3. Все 4 файла отвергаются Nomad 1.11.x — то есть деплой через `nomad job run` падал на каждый запуск. Закрыт 2026-04-18: во всех 4 .nomad файлах `checksum = var.CHECKSUM` обёрнут в `options { checksum = var.CHECKSUM }`. Локальный `nomad job validate` (v1.11.3) после правки возвращает `Job validation successful` для всех 4 файлов с реальным sha256-vars. Поднят как Critical: блокирует deploy pipeline целиком.

### High

- [x] **I-H1** — `deployments/infra/nomad/nomad.hcl:32` + `deployments/envs/prod/setup.sh:177` — `bootstrap_expect = 1` на каждой ноде, риск split-brain при одновременном первичном bootstrap. Закрыт 2026-04-17 через документацию (без изменений конфига): в `prod.md` после раздела «Добавление новой ноды» добавлен callout «Важно: первичное развёртывание нескольких нод одновременно» с правилом sequential bootstrap (`nomad server members` + `raft list-peers` перед запуском следующей ноды); комментарии в `nomad.hcl` (строки 28-37) и `setup.sh` (строки 161-165) дополнены предупреждением и ссылкой на prod.md. `bootstrap_expect=3` не используем: для расширения существующего кластера он не подходит, а добавлять режим переключения через ENV — лишняя сложность под одноразовую операцию.
- [x] **I-H2** — `.github/workflows/{gateway,xauth,xhttp,xws}-deploy.yml` — 4 deploy-workflow триггерятся `workflow_run: CI` параллельно и делают `git pull` в одну `/opt/platform` → race на `.git/index.lock`. Закрыт 2026-04-17: тело SSH-команды в `.github/actions/deploy-nomad/action.yml` обёрнуто во `flock` (`exec 9>/var/lock/platform-deploy.lock; flock -w 300 -x 9`). Серилизует параллельные деплои + любые ручные SSH-деплои; `/var/lock` = `/run/lock` на Ubuntu (tmpfs 1777) → нет stale lock после reboot, прав root не нужно. Дескриптор 9 закрывается автоматически на выходе из шелла, lock освобождается тоже автоматически. Выбран серверный flock вместо GitHub `concurrency` — единая точка изменения, одинаково защищает CI- и manual-источники.
- [x] **I-H3** — `.github/actions/deploy-nomad/action.yml:85` — `nomad job run $VAR_ARGS` подвержен word-splitting. Закрыт 2026-04-17 как побочный эффект I-C1: переход на `NOMAD_VAR_*` env (через `printf %q` + `export`) устранил `$VAR_ARGS` целиком — теперь команда выглядит как `nomad job run /opt/platform/deployments/infra/nomad/${NOMAD_FILE}` без пользовательских значений в argv. Word-splitting невозможен по построению.
- [x] **I-H4** — `deployments/envs/prod/setup.sh:265-272` — `wget` тарбола NATS без SHA256/GPG верификации. Закрыт 2026-04-17 минимальным фиксом: `wget` заменён на `curl -fsSL` (универсальнее — присутствует в большинстве минимальных образов); после скачивания тарбола скрипт тянет `SHA256SUMS` из того же релиза, выбирает строку нужного файла через `awk` и верифицирует через `sha256sum -c`. Хардкод версионных хешей сознательно отвергнут как плохой паттерн (привязка ops-артефакта к коду). Защищает от passive errors (CDN bitrot, transient corruption); от активного MITM через github.com TLS не защищает — общий канал — но это вне реалистичного threat model для одноразового bootstrap-окна (~5-10s). HashiCorp GPG fingerprint check для Nomad сознательно не делали — формально вне scope I-H4, риск аналогичный, при необходимости можно добавить отдельным финдингом.
- [x] **I-H5** — `.github/workflows/setup.yml:76-79, 87` — `scp/ssh -o StrictHostKeyChecking=no` на новый IP, MITM получает `NATS_CA_KEY` (корень доверия NATS-кластера) + `NOMAD_TOKEN` (root-доступ к Nomad) + произвольное исполнение через поддельный SSH-эндпоинт. Закрыт 2026-04-17 гибридом: добавлен optional `host_fingerprint` input в `workflow_dispatch`; в шаге Configure SSH через `ssh-keyscan` забираются host keys целевой ноды и пишутся в `~/.ssh/known_hosts`; если `host_fingerprint` задан — `ssh-keygen -lf` сверяет с ним каждый ключ, иначе TOFU + WARN-лог fingerprint для ручной сверки оператором. В шагах Copy/Run заменено `StrictHostKeyChecking=no` → `=yes` — теперь неизвестный/изменённый ключ блокирует подключение. По умолчанию защита уже строже текущей (TOFU вместо «принять любой»); при заполненном fingerprint — полная MITM-защита первого подключения.
- [x] **I-H6** — `deployments/envs/prod/setup.sh:231-251` (nomad.service) — Nomad-агент от root, `raw_exec`-tasks наследуют root → каждая компрометация сервиса = root. Закрыт 2026-04-17 через per-task user (recommended pattern для raw_exec, не строго по тексту аудита): добавлено `user = "nomad"` в task-блоки `gateway.nomad`, `xauth.nomad`, `xhttp.nomad`, `xws.nomad` (4 файла). Системный user `nomad` уже создаётся в `setup.sh:155` и владеет `/var/lib/nomad`; `/etc/nomad/env` уже chmod 600 (root-only). Nomad-агент остаётся root по причине: упрощает alloc-dir creation, cgroups, log-rotation, минимизирует риск сломать Nomad management; raw_exec через setuid запускает каждую task'у как nomad. Шаблон в `prod.md` («Добавить новый сервис») обновлён — теперь содержит обязательный `user = "nomad"` с ссылкой на I-H6, чтобы новые сервисы не теряли non-root статус. Замечание: raw_exec не даёт namespace/cgroup isolation между tasks — это его фундаментальное ограничение, для настоящей изоляции нужен переход на `exec` или `docker` driver (отдельная архитектурная задача, не I-H6).

### Medium

- [x] **I-M1** — `.github/workflows/ci.yml:124-130` — `gh release list | tail -n +$(( KEEP + 1 )) | xargs gh release delete` без `jq 'sort_by(.createdAt)'`. Порядок по имени: `build-9 > build-10` лексикографически → удаляются не старейшие. Fix: `gh release list --json name,createdAt | jq 'sort_by(.createdAt) | reverse | .[KEEP:]'`. **2026-04-18:** `tail -n +$(( KEEP + 1 ))` удалён, jq переписан с явным `sort_by(.createdAt) | reverse | .[$keep:]` (через `--argjson keep "$KEEP"`). Уточнение к формулировке: текущий `gh release list` фактически отдаёт записи в date-desc через REST API (а не в лексикографическом порядке), но контракт CLI этого не гарантирует — при ручном rebuild старого тега `created_at` опередил бы tag-номер и `tail` снёс бы не самые старые билды. Новый pipeline проверен на синтетическом входе (`build-1..10`, неперемешанные даты) — корректно оставляет 5 свежих, удаляет остальные. Поле `createdAt` уже было в `--json` — лишних правок не потребовалось.
- [x] **I-M2** — `.github/actions/deploy-nomad/action.yml:67-126` (аудитор указал :47 — ошибся номером) — DNS-перебор нод выбирает первую доступную по SSH, но не проверяет статус Nomad leader. Нода в процессе перезагрузки имеет живой SSH, но мёртвый Nomad. Fix: `curl -sf http://$HOST:4646/v1/status/leader` перед `nomad job run`. **2026-04-18:** добавлен SSH-probe `curl -sf http://127.0.0.1:4646/v1/status/leader` сразу после определения ARCH; на не-2xx или connection error → `continue` на следующую ноду. Уточнение к рекомендации аудита: probe против `$HOST:4646` с runner'а не работает — порт 4646 публично закрыт между runner и нодами per CLAUDE.md (firewall разрешает только node-to-node), curl упал бы по connection-timeout вне зависимости от состояния Nomad. Probe через SSH к localhost:4646 обходит firewall и использует штатный анонимный endpoint (без NOMAD_TOKEN). Один SSH round-trip (~50-200ms) вместо ~30s nomad job run timeout per «зомби»-ноду.
- [x] **I-M3** — `deployments/infra/nomad/gateway.nomad:64` — `count = 1` + `max_parallel = 1` → окно недоступности 10+ секунд при rolling update. Fix: `count = 2` для gateway, либо явно задокументировать accepted downtime. **2026-04-18:** `type = "service"` → `type = "system"`, `count = 1` удалён. System job автоматически распределяет один alloc на каждую client-ноду — соответствует семантике DNS RR + static port `:8080` (раньше при count=1 N-1 нод обслуживали `connection refused`). Rolling update через `max_parallel=1` даёт окно недоступности только на обновляемой ноде, остальные продолжают принимать трафик. Локальный `nomad job validate` подтверждает корректность (после фикса I-C4 на parallel scope). Демо xservices не трогали — у них нет static port, count=1 для демо допустим.
- [x] **I-M4** — `deployments/envs/prod/setup.sh:317` (аудит указал :299 — ошибся номером) — `openssl genrsa 2048` с сертификатом на `days 3650`. NIST рекомендует RSA-3072+ или ECDSA P-256 для 10-летнего срока. Fix: `openssl ecparam -name prime256v1 -genkey -out node.key`. **2026-04-18:** заменён `openssl genrsa ... 2048` на `openssl ecparam -name prime256v1 -genkey -noout -out ...`. ECDSA P-256 — NIST-current, защита до 2050+ (RSA-2048 deprecated после 2030). `-noout` обязателен — иначе openssl выводит EC PARAMETERS блок до приватного ключа, что может сбить некоторые TLS-парсеры. Smoke-test через `openssl ec -in node.key -text -noout` подтверждает корректный 256-bit ключ. CA-key (приходит из env) не трогали — алгоритм node-key и CA-key независимы. CSR и x509-sign команды без изменений (openssl auto-detect формат key-файла; алгоритм подписи берётся из CA-key). `days 3650` оставлен — short-lived certs + auto-rotation отдельная архитектурная задача.
- [x] **I-M5** — `deployments/envs/prod/setup.sh:293-327` — CA-ключ пишется в `/tmp/nats-ca.key`. Удаляется `rm -f` вместо `shred -u`. При OOM/crash в окне между созданием и удалением ключ остаётся на диске. Fix: `shred -u`, или использовать process substitution `<(...)` без создания файла. **2026-04-18:** реализован вариант с process substitution — устраняет класс уязвимости, а не симптом. Удалены `printf '%s\n' "$NATS_CA_KEY" > /tmp/nats-ca.key` и `chmod 600`, `-CAkey /tmp/nats-ca.key` заменён на `-CAkey <(printf '%s\n' "$NATS_CA_KEY")`, из `rm -f` убран `/tmp/nats-ca.key`. Bash подаёт openssl путь `/dev/fd/N`, ключ живёт только в памяти bash-процесса и пайпе — на ФС не попадает ни на один момент. `shred -u` не выбран: ненадёжен на SSD с wear-leveling и на journaled FS. Smoke-test (mktemp dir, эмуляция `NATS_CA_KEY` через env, имитация setup.sh-флоу): `openssl verify -CAfile ca.crt node.crt` → `OK`, SAN сохранён, в tmpdir после генерации файла CA-ключа нет. Упоминания `nats-ca.key` остались только в шапке-комментарии (инструкция как локально подготовить env-переменную) — это документация, не runtime.
- [x] **I-M6** — `deployments/envs/prod/prod.vars.example` — не содержит `INACTIVITY_TIMEOUT` (xws), `CACHE_TTL` (xhttp), `AUTH_ACCESS_TTL`/`AUTH_REFRESH_TTL` (xauth), `LOG_LEVEL`. При ручном `nomad job run -var-file` они остаются на дефолтах без напоминания. Fix: добавить все объявленные в `.nomad`-файлах переменные с комментарием про дефолт. **2026-04-18:** в секцию `# xauth` добавлены `auth_access_ttl = "15m"` и `auth_refresh_ttl = "168h"`, в `# xhttp` — `cache_ttl = "30s"`, заведена новая секция `# xws` с `inactivity_timeout = "3m"`. Все значения — реальные дефолты из соответствующих `.nomad`-файлов с inline-комментарием `# default из <service>.nomad`. Уточнение к формулировке аудита про `LOG_LEVEL`: он уже был в шаблоне (общий, секция Gateway, строка 32) — для всех сервисов передаётся одним флагом, отдельные per-service log_level не нужны (лишний шум для дефолтного use-case). Сверка через awk-pipeline по всем 4 `.nomad` подтвердила: единственная необъявленная переменная — `checksum`, но это runtime-значение CI (SHA256 артефакта), для ручного деплоя не подставляется. Авто-генератор отвергнут как overkill — файл правится 1-2 раза в год.
- [x] **I-M7** — `deployments/envs/prod/prod.vars.example:11-18` ссылается на `platform.nomad` / `xservices.nomad`, которых не существует (разделены на gateway.nomad + xauth.nomad + xhttp.nomad + xws.nomad). Fix: обновить примеры команд. **2026-04-18:** примеры заменены на 4 актуальных команды с явным разделением: `# Платформа (всегда):` → `gateway.nomad`, `# Демо-сервисы (опционально, не для прод-продуктов):` → `xauth/xhttp/xws.nomad`. Соответствует CLAUDE.md (`x*` — демо-примеры, не для прода). Команды свёрнуты в одну строку (раньше были многострочные с `\` — труднее копировать).
- [x] **I-M8** — `deployments/envs/dev/dev.md:49-65` — те же ссылки на устаревшие имена джобов. Fix: обновить. **2026-04-18:** scope шире, чем формулировка аудита — не только имена `platform`/`xservices`, но и `-var="binary_dir=$PWD/bin"` (переменная нигде в `.nomad` не объявлена) и сам `platform.nomad` (файла нет). Реальный dev-workflow: `start.sh` рендерит 4 HEREDOC-варианта в `/tmp/dev-{gateway,xauth,xhttp,xws}.nomad` с inline-подстановкой dev-значений и запускает их (`start.sh:446-449`); канонические `deployments/infra/nomad/*.nomad` рассчитаны на `artifact { source = github.com/... }` и в dev не подходят. Правки: блок `nomad job status` (строки 37-40) — реальные 4 имени с пометкой платформа/демо; раздел «Переменные окружения» — `start.sh stop && start.sh` вместо несуществующего `nomad job run ... platform.nomad`, добавлено упоминание `/tmp/dev-*.nomad`; раздел «Rolling update» — `go build -o bin/gateway && nomad job run /tmp/dev-gateway.nomad` (использует уже сгенерированный start.sh файл). Новую находку не открывал — суть та же (dev.md рассинхронизирован), просто формулировка аудита не охватывала весь дрейф.
- [x] **I-M9** — `.github/workflows/setup.yml` передавал `INSTALL_POSTGRES`, но `setup.sh` его никогда не читал (закрыт 2026-04-17 вместе с I-C3: input `install_postgres` и его пропуск в env-файл удалены целиком. PostgreSQL — зависимость демо-сервиса xhttp, в платформенном CI ему места нет; настройка БД остаётся за демо-руководством. Аудитор ошибся в номере строки — на `setup.sh:39` находится `NOMAD_TOKEN`, не `INSTALL_POSTGRES`; в setup.sh переменная вообще никогда не упоминалась).
- [x] **I-M10** — `.github/actions/deploy-nomad/action.yml:93-101` — `NOMAD_TOKEN` проходит `printf '%q'` и вставляется в `bash -c "..."` SSH-аргумент. При включённом `sshd` verbose на сервере или `set -x` на клиенте токен попадает в логи. Fix: передавать через stdin pipe (`ssh ... 'bash -s' <<< "$script"` с экспортом env через `export NOMAD_TOKEN=$TOKEN` в начале, где `$TOKEN` подставляется в heredoc с кавычками `<<"EOF"`). **2026-04-18:** реализован переход на stdin pipe. Раньше длинная строка-скрипт (с `export NOMAD_TOKEN=$Q_NOMAD_TOKEN` и развёрнутыми `NOMAD_VAR_*`) уходила как argv-аргумент `ssh`'у — была видна в `ps -ef | grep ssh` на runner'е и могла попадать в `/var/log/auth.log` при `LogLevel >= VERBOSE` на удалённой ноде. Теперь скрипт собирается в `REMOTE_SCRIPT="..."` и подаётся через here-string: `$SSH "${DEPLOY_USER}@${HOST}" 'bash -s' <<< "$REMOTE_SCRIPT"`. SSH argv теперь содержит только `ssh ... bash -s` — токен и все NOMAD_VAR_* values уходят на remote bash через отдельный stdin-канал. Уточнение к рекомендации аудита про `<<"EOF"`: совет был внутренне противоречив (quoted heredoc предотвращает локальную подстановку — `$Q_NOMAD_TOKEN` остался бы литералом и токен не дошёл бы); реализована корректная вариация — обычная переменная + here-string, локальная подстановка `$Q_NOMAD_TOKEN` происходит при формировании `REMOTE_SCRIPT`, на remote приходит уже готовый скрипт. `printf '%q'` для NOMAD_TOKEN сохранён — защита от shell-injection при спец-символах в токене на стороне remote bash остаётся актуальной независимо от способа доставки. YAML + bash syntax файла валидированы (`ruby -ryaml` extract → `bash -n`); smoke-test с токеном содержащим `$pecial chars` подтвердил корректную доставку и работу `ENV_EXPORTS` через stdin-pipe.

### Low

- [x] **I-L1** — `deployments/infra/nats/nats.conf` — cluster-блок защищён только mTLS, без `cluster { authorization { user/password } }`. При компрометации одного cert можно подключиться без пароля. Fix: добавить cluster authorization или зафиксировать в угрозах что mTLS достаточно. **2026-04-18:** выбрана опция «зафиксировать в угрозах» (audit предусматривает обе). В `nats.conf` cluster-блок добавлен 3-строчный комментарий-маркер про сознательное отсутствие cluster authorization со ссылкой на `prod.md`. В `prod.md` добавлен раздел `## Безопасность кластера NATS`: что защищает текущая конфигурация (verify=true + off-server CA-key + per-host cert), почему cluster authorization не добавлен (защищал бы только сценарий «утёк node.key но не env-файл», а в реальности оба под chmod 600 на одном диске и берутся из одного root-доступа) и условия пересмотра (multi-tenant, HSM-хранение key, compliance). Реальный новый attack surface: дополнительный секрет в env с риском рассинхрона между нодами при ротации — без покрытия threat-векторов. Альтернативный вариант (defense-in-depth) отвергнут осознанно: цена > выгода в текущей threat model.
- [x] **I-L2** — `deployments/infra/nomad/nomad.hcl` — нет `raft_multiplier`. Для multi-region или нестабильной сети рекомендуется `raft_multiplier = 5`. Fix: добавить при расширении на multi-DC. **2026-04-18:** в `nomad.hcl` server-блок добавлен `raft_multiplier = 5` с пояснительным комментарием (×5 на heartbeat/election/leader_lease, failover 5-25s вместо 1-5s — приемлемо, платформа не realtime). Audit-формулировка «добавить при расширении на multi-DC» переосмыслена после уточнения от пользователя: multi-DC — целевой сценарий платформы с первого дня, а не отложенный, поэтому defer-документация не подходит — оператор не должен ловить re-election storm под нагрузкой prod-инцидента. Default=1 рассчитан на LAN (<10ms), на WAN-jitter (cross-DC 50-300ms, transient loss) флапает. Один и тот же конфиг теперь работает в single-DC и multi-DC без правок. В `prod.md` раздел `## Масштабирование` дополнен подразделом «Разные дата-центры — first-class сценарий»: явно зафиксировано, что `setup.sh`/`nomad.hcl`/`nats.conf`/env-файлы одинаковы для всех нод; единственное, что меняется при добавлении ноды в новом DC — DNS A-запись `PLATFORM_DOMAIN`; требования к сети (4222/6222/4646-4648 открыты, NAT-traversal не нужен — `cluster_advertise`/`advertise` адресуют публичным IP). `nomad config validate` → `Configuration is valid!` (warnings про mTLS и bootstrap — pre-existing, не связаны с правкой).
- [x] **I-L3** — `deployments/envs/prod/setup.sh:460-465` — `sleep 2` между запуском NATS и Nomad. При тяжёлом JetStream recovery ненадёжно. Fix: `until nats-server --signal ldm 2>/dev/null; do sleep 1; done` или `systemctl is-active nats`. **2026-04-18:** реальная строка — `setup.sh:486` (аудитор ошибся в номере). `sleep 2` заменён на polling-цикл `until curl -sf --max-time 2 http://127.0.0.1:8222/healthz` с timeout 60s — JetStream-aware (endpoint `/healthz` отдаёт 200 только после восстановления stream/KV-store), симметрично с уже существующим Nomad-poll'ом в `bootstrap_acl()` (setup.sh:504-508). Audit-варианты не выбраны: `nats-server --signal ldm` — это lameduck-mode trigger, не readiness probe; `systemctl is-active` возвращается сразу после fork-а, до открытия портов и JetStream recovery. Порт `8222` hardcoded с комментарием «совпадает с http_port в nats.conf» — та же convention, что для 4222/6222/8080 в setup.sh (порты раскиданы константами по скрипту, синхронизация ручная). Парсинг `nats.conf` отвергнут: fragile к смене формата (комменты, многострочные блоки), overkill для одной константы. TCP-probe порта 4222 отвергнут: открывается до завершения JetStream recovery — не закрывает audit-причину. `bash -n` чисто. Промежуточный лог «Ожидание готовности NATS» + финальный лог `(готов через ${elapsed}s)` дают оператору видимость длительности recovery.
- [x] **I-L4** — `.github/workflows/ci.yml:82,96` — `ls cmd/` выдаёт файлы вместе с директориями. Любой файл в `cmd/` (README, .gitkeep) сломает build. Fix: `find cmd -maxdepth 1 -mindepth 1 -type d -printf '%f\n'`. **2026-04-18:** оба места («Build release binaries» и «Compute checksums») переписаны на bash-glob: `shopt -s nullglob; for dir in cmd/*/; do svc="${dir%/}"; svc="${svc#cmd/}"; ... done`. Trailing slash в glob + nullglob раскрывают выражение только в реальные директории; файл в `cmd/` (README/.gitkeep/.DS_Store) не попадает в цикл. Audit-fix через `find cmd -maxdepth 1 -type d` не выбран: зависит от GNU `-printf` (ubuntu-latest есть, но лишняя внешняя зависимость для one-liner-а, который штатно выражается bash-глобом). YAML синтаксис проверен через `ruby -ryaml`; smoke-test на реальной `cmd/` выдал ожидаемый список из 4 сервисов (gateway, xauth, xhttp, xws). Комментарий добавлен, чтобы follow-up контрибьюторы не возвращались к `ls cmd/`.
- [x] **I-L5** — `deployments/envs/prod/setup.sh` — `install_nomad` пропускает установку если `nomad` уже в PATH, без проверки версии. Повторный запуск не обновит. Fix: сравнить `nomad version` с ожидаемой. **2026-04-18:** scope расширен — та же проблема в `install_nats` (skip без version-check), audit упомянул только Nomad. Реализован асимметричный подход: **NATS** — auto-upgrade. В `install_nats` распарсен `nats-server --version` через `grep -oE '[0-9]+\.[0-9]+\.[0-9]+'`, сравнение с `$NATS_VERSION`; при несовпадении `needs_restart=1`, install переустанавливает (`mv` overwrites `/usr/local/bin/nats-server`), затем условный `systemctl restart nats` если unit активен (на первой установке unit ещё не создан в `setup_nats` — restart пропускается, старт случится в `start_services`). **Nomad** — accept-risk + комментарий: pinned `NOMAD_VERSION` нет (ставится из HashiCorp APT = latest на момент первой установки). Audit-вариант «сравнить с ожидаемой» underspec: ожидаемой версии в скрипте нет, ввести `NOMAD_VERSION` + apt-pin (`apt-get install -y nomad=${VER}*`) — несоразмерное расширение для accept-risk-сценария (повторный запуск setup.sh ради upgrade — antipattern; реальный upgrade-flow = `apt install --only-upgrade nomad` + `systemctl restart`, rolling по нодам). Комментарий в skip-ветке `install_nomad` явно указывает корректную ops-процедуру. `bash -n` чисто; smoke-test парсинга версии: same → skip, different → re-install. По NATS audit закрыт полностью; по Nomad — документированный accept-risk.
- [x] **I-L6** — `deployments/infra/nomad/nomad.hcl` — `bind_addr = "0.0.0.0"` для HTTP API. Защищён ACL, но при откате ACL bootstrap или misconfig API незащищён. Fix: `bind_addr = "127.0.0.1"` + advertise public_ip (уже есть), либо включить Nomad TLS. **2026-04-19:** audit-формулировка `bind_addr = "127.0.0.1"` отвергнута как ломающая — `bind_addr` управляет всеми тремя listener'ами (HTTP/RPC/Serf), смена на 127.0.0.1 убила бы inter-node RPC/Serf. Реализован hashicorp-каноничный паттерн: `bind_addr = "0.0.0.0"` сохранён, добавлен блок `addresses { http = "127.0.0.1" }` после `bind_addr` — переопределяет адрес только HTTP-listener'а, RPC (4647) и Serf (4648) остаются на 0.0.0.0 для inter-node трафика. Доступ к Nomad UI/API теперь через SSH-tunnel: `ssh -L 4646:127.0.0.1:4646 user@node` (отражено в `print_summary` и комментариях). При откате ACL bootstrap/misconfig порт 4646 наружу не открывается даже временно. Правка применена в обоих местах (см. ниже про drift). Попутно засинхронен drift I-L1 и I-L2 в `deployments/envs/prod/setup.sh` HEREDOC: оригинальные правки I-L1 (cluster.authorization comment) и I-L2 (`raft_multiplier = 5` + multi-DC comment) затронули только канонические `deployments/infra/nats/nats.conf` и `deployments/infra/nomad/nomad.hcl`, но фактически развёртываются HEREDOC-копии из `setup.sh:391-428` и `setup.sh:173-230` — drift висел до этого фикса. Все три правки выполнены одной волной, чтобы не плодить пустые commit'ы. На будущее: любые изменения файлов в `deployments/infra/{nomad,nats}/` должны одновременно применяться в соответствующем HEREDOC внутри `deployments/envs/prod/setup.sh` (архитектурная задача консолидации — отложена осознанно). Правило сохранено в feedback memory. `nomad config validate` → `Configuration is valid!`; `bash -n setup.sh` → OK.
- [x] **I-L7** — `deployments/infra/nomad/{xauth,xhttp,xws}.nomad` — нет `service { check { ... } }`. Nomad считает аллокацию healthy сразу после старта процесса. Fix: HTTP-check на `/health` для xhttp (если открыть) или `script`-check (`nats pub --server ... api.v1.xX.ping`). **2026-04-19 deferred:** аудит-варианты (HTTP-check без endpoint в сервисе, script через `nats` CLI с mTLS-cert в env task) — оба требуют production-hardening x*-сервисов или новой ноды-зависимости. При обсуждении обнаружено фундаментальное ограничение: TCP/PING-probe не отличает healthy от deadlock-в-handler; корректный probe должен идти через тот же NATS-dispatcher (см. P-M9 для деталей). Эскалировано в платформенный пункт **P-M9** «built-in health endpoint в `internal/platform/nc`» — обёртка регистрирует `_health` handler через основной mux + HTTP /healthz с self-roundtrip. Закрытие P-M9 автоматически закрывает I-L7 (helper применится к x* демо одной правкой). **2026-04-23:** закрыто вслед за P-M9 — `nc.RegisterHealth` + `service { check { type = "http", path = "/healthz" } }` в 3 nomad-джобах, dev-валидация прошла (`/healthz → 200`, Nomad service-checks active).
- [x] **I-L8** — `deployments/envs/prod/setup.sh:128-131` — `apt-get install` без `--no-install-recommends`. Fix: добавить флаг. **2026-04-19:** scope расширен — флаг добавлен ко всем трём `apt-get install` в скрипте: `install_base()` (curl/wget/git/unzip/gnupg/lsb-release/ufw/dnsutils, :130), `install_nomad()` (:155), `generate_nats_certs()` fallback openssl (:344). Также синхронизирован комментарий-пример upgrade-команды (:142). Все перечисленные пакеты сохраняют функциональность без recommends — `curl`/`dig`/`lsb_release`/`ufw`/`gpg` доступны через `Depends:`. Эффект: меньше дисковой занятости, не подтягиваются вспомогательные демоны (`rsyslog`, `policykit-1`, `cron-цикл`), уменьшен attack surface на bootstrap'е prod-VPS. Объёмный комментарий добавлен в `install_base()`, чтобы будущие контрибьюторы понимали почему. `bash -n setup.sh` → OK.
- [x] **I-L9** — `docs/production-setup.md:64-83` — таблица GitHub Secrets не содержит `ACCESS_SECRET` (xhttp), `COOKIE_SECURE`. `prod.md` содержит полный список → рассинхрон. Fix: синхронизировать. **2026-04-19:** scope расширен от audit-формулировки. При сверке обнаружено больше дрейфов: помимо `ACCESS_SECRET`/`COOKIE_SECURE` отсутствовали Variables `AUTH_ACCESS_TTL`/`AUTH_REFRESH_TTL`/`INACTIVITY_TIMEOUT`/`CACHE_TTL` (добавлены в `prod.md` по I-M6); упомянут устаревший workflow input `install_postgres` (удалён по I-M9); не упомянут `host_fingerprint` (добавлен по I-H5). Корневая причина — два markdown-документа с одной целью (`docs/production-setup.md` упрощённый guide vs `deployments/envs/prod/prod.md` детальный): синхронизация вручную = гарантированный рассинхрон при любой правке secrets/inputs. Реализован вариант устранения класса проблемы: `docs/production-setup.md` удалён целиком, `deployments/envs/prod/prod.md` остаётся единственным источником правды для prod-инструкций. Внешних ссылок (`grep -r production-setup .`) на удалённый файл не найдено — только сам audit-пункт. Папка `docs/` теперь содержит только `audit/`. Минимальный фикс (точечная синхронизация двух таблиц) отвергнут — рассинхрон вернулся бы при следующей правке secrets.

---

## Demo

### High

- [x] **D-H1** — `cmd/xhttp/main.go:69` — `AccessSecret: []byte(os.Getenv("ACCESS_SECRET"))` без проверки на пустоту → пустой HMAC-ключ принимает любой токен, подписанный таким же пустым. Закрыт 2026-04-17 минимальным inline-фиксом: до построения `authCfg` в `main.go` добавлен check `if accessSecret == "" { log.Fatal()... }` с подсказкой что значение должно совпадать с `AUTH_ACCESS_SECRET` xauth. Не выносили в `xhttp.LoadConfig` как в xauth — xhttp demo, минимизация изменений.
- [x] **D-H2** — `internal/services/xws/manager.go:60-64` + `session.go:66` — data race на `session.timer`. `time.AfterFunc`-коллбэк читает/вызывает `sess.close()`, `resetTimer()` вызывает `timer.Reset(...)` из другой горутины. Без мьютекса. Fix: добавить `sync.Mutex` в `session`, брать его в `resetTimer()` и в начале коллбэка. **2026-04-17:** в `session` добавлены `mu sync.Mutex` + `closed bool`; `resetTimer()` под мьютексом и no-op после close; `close()` под мьютексом стопит таймер. Закрыто совместно с D-H3 одной правкой `internal/services/xws/session.go`.
- [x] **D-H3** — `internal/services/xws/manager.go:113-115` vs `manager.go:60-64` — одновременный disconnect-сообщение и истечение таймера → двойной `sess.close()` → двойной `Unsubscribe` + двойной CLOSE publish. Не фатально, но создаёт ошибки в логах. Fix: `sync.Once` в `session.close()`. **2026-04-17:** вместо `sync.Once` — `closed bool` под `mu` (тот же мьютекс, что и для D-H2): `close()` идемпотентен, повторные вызовы — ранний return. Избыточные `sess.timer.Stop()` в `manager.go:113,137` оставлены (безопасны). Закрыто совместно с D-H2.
- [x] **D-H4** — `internal/services/xauth/handlers.go:108-116` — `HandleRefresh` сначала делает `PutValue(oldJTI, "revoked")`, потом выдаёт новые токены через `issueTokenCookies`. Если выдача новых упадёт — старый JTI отозван, новых нет → клиент теряет сессию навсегда. Fix: поменять порядок — сначала выдать новые, потом отозвать старый. **2026-04-17:** свопнут порядок — `issueTokenCookies` идёт первой; revoke старого JTI делается после, и его сбой только пишется в `Warn`-лог (не возвращает 500). Trade-off: сбой revoke оставляет старый JTI валидным до его `Exp` (replay window), но это сильно лучше потери сессии — новые куки клиент уже получил.

### Medium

- [x] **D-M1** — `internal/services/xhttp/cache.go:49` — `Invalidate` пишет пустой `[]byte{}` вместо `kv.Delete(key)`. Зависит от того, вернёт ли NATS SDK `entry.Value() == nil` или `[]byte{}` для такой записи. Fix: `kv.Delete(key)` (доступен с nats.go v1.x). **2026-04-18:** в `internal/platform/nc/client.go` добавлен платформенный примитив `Delete(ctx, bucket, key) error` — обёртка над `kv.Delete`, рядом с GetValue/PutValue/WatchKey, тот же стиль (fmt.Errorf wrap, «бакет %q» / «ключ %q»). В `cache.go:47-53` `c.nc.PutValue(..., []byte{})` заменён на `c.nc.Delete(...)`; комментарий переписан — упоминает `purge marker` и пояснение, что subsequent Get вернёт nil через ErrKeyNotFound branch в `nc.GetValue`. Бакет больше не растёт пустыми entry. Уточнение: `nc.Delete` добавлен в платформенный слой, не в xhttp — это generic KV-примитив (CLAUDE.md: «Platform code must not reference x* services» соблюдено, метод не специфичен). `kv.Delete` идемпотентен в jetstream-пакете nats.go, удаление несуществующего ключа не ошибка. Audit-комментарий «NATS KV API не гарантирует наличия Delete во всех версиях Go SDK» был фактически некорректен — Delete доступен с nats.go v1.x в `jetstream` пакете, который и используется (`p.JS.KeyValue`). `go build ./... && go vet ./...` — чисто.
- [x] **D-M2** — `internal/services/xauth/jwt.go:48-75` — `VerifyJWT` не проверяет `parts[0]` против ожидаемого header. Прямой вектор `alg:none` не работает (HMAC-compare пустой строки с expected вернёт false), но отсутствие явной проверки — code smell. Fix: `if parts[0] != jwtHeader { return Claims{}, errors.New("unexpected header") }`. **2026-04-18:** добавлена литеральная проверка `parts[0] != jwtHeader` сразу после `len(parts) != 3` и до HMAC-вычисления (jwt.go:54-60). Возврат через `fmt.Errorf("jwt: неподдерживаемый header")` — консистентно с остальным файлом (`fmt` уже импортирован, без `errors.New` чтобы не плодить импорты ради одной строки). Выгоды: (1) defense-in-depth — если в будущем кто-то добавит alg-dispatch по header, дыра сразу не появится; (2) fail-fast — не тратим CPU на HMAC от заведомо чужого токена. Существующий HMAC-check уже отбрасывал подделки без секрета (атака неэксплуатируема и до фикса), audit это явно отмечал — это hygiene/code smell, не CVE. `go build ./... && go vet ./...` чисто.
- [x] **D-M3** — `internal/services/xws/manager.go:136-139` — `CloseAll` держит `Manager.mu` во время `nc.PublishMsg` и `Unsubscribe` каждой сессии. При медленном NATS блокирует весь менеджер. Fix: под локом — снимок списка сессий; закрытие — вне лока. **2026-04-18:** реализован snapshot-pattern. Под `m.mu`: копируем `*session`-указатели в slice + очищаем `m.sessions = make(...)`, релиз лока. Вне лока: `sess.timer.Stop() + sess.close()` для каждой сессии. Лок теперь удерживается O(N) на копирование указателей, а не O(N × NATS_latency) на сетевые операции. Безопасность: `session.close()` идемпотентен (sess.mu + closed-флаг, см. session.go:61-66) — параллельный вызов из `handleIncoming` disconnect-ветки не дублирует PublishMsg/Unsubscribe; параллельный `m.remove(sid)` для уже-отсутствующего sid — no-op (delete на map). Параллельный `Open(sid)` после snapshot теоретически создаст новую сессию, но shutdown в процессе (CloseAll вызывается в `cmd/xws/main.go:86` непосредственно перед `natsClient.Drain` на SIGTERM) — нерелевантно. Добавлен docstring с обоснованием. `go build ./... && go vet ./...` чисто.
- [x] **D-M4** — `internal/services/xhttp/handlers.go:147-174` — thundering herd на cache-miss. N параллельных запросов уходят в БД одновременно. Fix: `singleflight.Group`. **2026-04-18 wontfix:** xhttp — демо-сервис; CLAUDE.md прямо запрещает production-hardening `x*`-сервисов («Do not spend effort on production-quality hardening of x* services»). Реализация audit-fix потребовала бы добавить новую прямую зависимость `golang.org/x/sync/singleflight` в платформенный `go.mod` ради единственного демо-обработчика — антипаттерн. Тот же thundering-herd паттерн присутствует в `HandleGet:80-128` (audit его не упомянул) — оба места осознанно остаются неисправленными. Реальные бизнес-сервисы, построенные поверх платформы, могут добавить `singleflight` в свой собственный `go.mod`, если их load-profile требует — это не платформенный примитив. Audit-находка корректна по существу, но цена фикса (платформенная зависимость) непропорциональна выгоде (защита демо от нагрузки, которую демо испытывать не должно).
- [x] **D-M5** — `internal/services/xauth/handlers.go:87` (`HandleRefresh`) и `:175` (`HandleMe`) — `time.Now().Unix() > c.Exp` без clock-skew tolerance. Inconsistency с `internal/middleware/xauth.go:72`, где P-H5 закрыл ту же проверку через константу `accessTokenClockSkew = 60s`. Те же ноды, тот же NTP-дрейф: токен, выданный xauth на ноде A в момент `Exp - 1s`, на ноде B при checking может оказаться «истёкшим» из-за рассинхрона часов на пару секунд. **2026-04-25:** константа вынесена в `internal/services/xauth/jwt.go` как `JWTClockSkew = 60 * time.Second` (public, для импорта из middleware). `accessTokenClockSkew` в `internal/middleware/xauth.go` удалён, проверка перешла на `xauth.JWTClockSkew`. В handler'ах `HandleRefresh:87` и `HandleMe:175` добавлен тот же `+ int64(JWTClockSkew.Seconds())`. Имя константы общее (не `AccessTokenClockSkew`) — она применяется и для access (15м TTL), и для refresh (168ч TTL); 60s skew одинаково корректен для обоих по RFC 7519 §4.1.4. Цикл импортов отсутствует: `xauth` — пакет данных, `middleware` импортирует `xauth` (исходно так), обратной зависимости нет. `go build ./... && go vet ./...` чисто.

### Low

- [x] **D-L1** — `internal/services/xhttp/handlers.go:56-77` — `HandleCreate` не валидирует пустой `name`. Fix: `if req.Name == "" { ReplyError(msg, 400, ...) }`. **2026-04-19:** добавлена проверка сразу после `json.Unmarshal` — пустой `name` отбивается с `400 "name required"` до похода в БД (схема `xhttp.name TEXT` без NOT NULL/CHECK иначе создавала бы мусорные строки). `value=""` оставлен валидным — может быть осмысленный use-case (запись с пустым значением); audit это поле тоже не упоминает. `HandleUpdate` не трогали — отдельная находка не открыта, при необходимости откроется при следующем audit'е (демо, не критично). `go build ./... && go vet ./...` чисто.
- [x] **D-L2** — `cmd/xws/main.go:54-66` — `sid` не валидируется. `.` или `*` создадут NATS-subject с wildcards. Fix: regex `^[a-zA-Z0-9_-]{8,64}$`. **2026-04-19:** добавлен пакетный `validSID = regexp.MustCompile(` + "`" + `^[a-zA-Z0-9_-]{8,64}$` + "`" + `)` (точно по audit) с пояснительным комментарием. Проверка вставлена в connect-handler сразу после извлечения `sid` (header / json fallback) и до `mgr.Open(sid)` — невалидный формат отбивается с `Warn`-логом и return, до создания подписки на `api.v1.xws.ws.in.<sid>`. Вариант с узким `^[a-f0-9]{16}$` (точный текущий формат Gateway: 8 байт CSPRNG → 16 hex) отвергнут: связал бы xws с конкретной деталью Gateway, при смене формата (base64url, длина) пришлось бы править оба сервиса. Defense-in-depth обоснован — Gateway-CSPRNG обеспечивает безопасный sid в штатном flow, но xws как самостоятельный NATS-listener защищается от любого источника connect-сообщений (компрометация другого сервиса с NATS-доступом). `go build ./... && go vet ./...` чисто.
- [x] **D-L3** — `cmd/xhttp/main.go:69` + `internal/services/xhttp/config.go` — `ACCESS_SECRET` читается напрямую через `os.Getenv`, не через `Config`. Нарушение паттерна. Fix: добавить поле `AccessSecret []byte` в Config. **2026-04-19:** в `internal/services/xhttp/config.go` добавлено поле `AccessSecret []byte` в `Config`-struct и блок чтения `ACCESS_SECRET` через `os.Getenv` в `LoadConfig` (fail-fast при пустом значении — стиль идентичен существующему чтению `DATABASE_URL`); ENV-документация в верхнем комментарии файла дополнена строкой про `ACCESS_SECRET`. В `cmd/xhttp/main.go` удалён локальный блок `accessSecret := os.Getenv(...)` + Fatal-проверка (5 строк); `authCfg.AccessSecret` теперь берётся из `cfg.AccessSecret`. Связанная D-H1-проверка (fail-fast на пустоту) сохранилась — переехала в `LoadConfig`. Импорт `os` в main.go остался (нужен для `os.Signal` в shutdown-канале). Семантика идентична предыдущему поведению, паттерн «вся конфигурация — через `LoadConfig`» восстановлен — соответствует CLAUDE.md «All configuration is loaded from environment variables only. Each service has a `LoadConfig()`». `go build ./... && go vet ./...` чисто.

---

## Глобальные рекомендации

- [x] **G1** — Нет CI-проверки на `go 1.26.1` → добавить `go vet`, `staticcheck`, `govulncheck` в `.github/workflows/ci.yml` отдельной джобой. **2026-04-25:** `go vet ./...` уже выполнялся в ci-джобе (строка 43 — формулировка audit-пункта была неточна). Добавлена отдельная джоба `lint` параллельно с `ci`: `dominikh/staticcheck-action@v1` (`install-go: false`, переиспользует setup-go из шага выше) + `golang/govulncheck-action@v1`. `release.needs` обновлён с `ci` на `[ci, lint]` — pre-release создаётся только если оба зелёные. Действия выбраны вместо `go install` — boilerplate меньше, обновления автоматом через G7 (dependabot github-actions). Оба тула fail-on-error по умолчанию (warn без блокировки бессмыслен — никто не пойдёт смотреть). YAML валидирован `ruby -ryaml`. Локальный прогон выявил 2 находки, обе закрыты в этом же пункте: (1) `staticcheck SA4011` в `internal/services/gateway/handlers.go:269` — ineffective `break` внутри `select` (выходил из select, не из for; case `time.After` через `continue` спасал, но автор явно хотел выход из for). Рефакторинг: labeled break — `retry: for { ... break retry ... }`, удалён дублирующий хвостовой `break` и пустой `continue`. Поведение не изменилось, семантика стала явной. (2) `govulncheck` — 4 stdlib CVE (crypto/x509 ×3, crypto/tls ×1) в `go1.26.1`, fixed в `go1.26.2`. Bump `go 1.26.1 → go 1.26.2` в `go.mod`. `GOTOOLCHAIN=auto` (default) → `go build` автоматически скачал toolchain `go1.26.2`, явный `go install golang.org/dl/go1.26.2` не нужен. `go.sum` без изменений (toolchain bump его не трогает). Финальный прогон: `go build`, `go vet`, `staticcheck`, `govulncheck` — все зелёные.
- [x] **G2** — Нет `.golangci.yml` — включить базовый набор линтеров (errcheck, gosec, bodyclose, contextcheck, rowserrcheck). **2026-04-25:** создан `.golangci.yml` (v2 schema, golangci-lint v2.11.4 pinned). Linters: `default: standard` (errcheck, govet, ineffassign, staticcheck, unused) + audit-набор (bodyclose, contextcheck, gosec, rowserrcheck). Standalone `staticcheck-action` убран из `ci.yml` — staticcheck теперь идёт через golangci-lint, чтобы не вести две точки конфигурации (отставание lint-action от свежего staticcheck считаем приемлемым). Добавлены exclusions под целевое состояние «тесты будут все виды» (см. memory `feedback_tests_planned`): `_test.go` — errcheck off, gosec G404|G101 off (math/rand и hardcoded creds в фикстурах допустимы). errcheck.exclude-functions: типовые паттерны `(io.Closer).Close`, `(*sql.DB).Close`, `(*sql.Rows).Close`, websocket `SetReadDeadline`/`SetWriteDeadline`, nats `Subscription.Unsubscribe` — централизованный whitelist вместо `_ = X.Close()` в десятке мест. `gosec.excludes: [G104]` — G104 дублирует errcheck, чтобы не дублировать список. Локальный прогон выявил находки, все закрыты в этом же пункте: (1) errcheck/gosec G104 на `defer Close()` и `SetXxxDeadline` — закрыты конфигом; (2) `safeWrite` (local closure) на line 589 без `_ =` — приведено к стилю остальных call sites (502, 560); (3) gosec G705 «XSS via taint analysis» на `w.Write(resp.Data)` (gateway-проксирование NATS-ответа) — `//nolint:gosec` с пояснением, что данные приходят из доверенного backend, не от пользователя; (4) contextcheck на `gw.handleWS(...)` — рефакторинг: `handleWS` теперь принимает `parentCtx context.Context` явно (`gw.ctx` от server lifecycle), вызов помечен `//nolint:contextcheck` с пояснением — `r.Context()` здесь использовать нельзя (отменяется по выходу из handler'а, а WS должен жить до shutdown). Финальный прогон: `go build`, `go vet`, `golangci-lint run`, `govulncheck` — все зелёные. CI-джоба `lint` обновлена: `golangci/golangci-lint-action@v6` (pinned `v2.11.4`) + сохранён `golang/govulncheck-action@v1`.
- [x] **G3** — Нет тестов нигде (`CLAUDE.md` явно говорит "No tests exist yet"). Минимум — table-tests на `utils/hosts.go`, `utils/env.go`, `ratelimit.go` (LRU логика); integration на gateway (HTTP→NATS) через `nats-test-server` или тестовый NATS в docker. **2026-04-25:** созданы фундамент тестов под целевое состояние «все виды» (см. memory `feedback_tests_planned`). Unit: `utils/hosts_test.go` (ParseAllowedHosts × 9 кейсов, AllowedHostSet.Allows × 6 кейсов + empty-set), `utils/env_test.go` (string/int/bool/float/duration + fallback-сценарии при невалидных значениях, включая time.Duration без единицы измерения), `internal/services/gateway/ratelimit_test.go` (LRU semantics: создание entry, перемещение в front при доступе, eviction oldest при заполнении, переиспользование limiter для одного IP, cleanupExpired по back-walk до первой свежей записи; realIP × 5 кейсов c trusted proxy / X-Real-IP). Integration: `internal/services/gateway/gateway_integration_test.go` с build tag `//go:build integration` — embedded NATS через `github.com/nats-io/nats-server/v2/test.RunRandClientPortServer` (без docker, без внешних сервисов), два теста: HTTP→NATS round-trip (POST /v1/echo/ping, проверка тела/заголовков ответа, X-Request-Id) и /health endpoint при подключённом NATS. CI обновлён: `go test -race ./...` + отдельный шаг `go test -race -tags=integration ./...`. Postgres-зависимые тесты (для xhttp) появятся как отдельный шаг с GHA `services: postgres:17` когда понадобится. `nats-server/v2` v2.12.7 добавлен в go.mod как direct require — используется только из test-кода, в production-бинарники не попадает (test-файлы исключены из `go build ./cmd/...`). CLAUDE.md раздел Testing переписан с актуальной структурой и командами. Финальный прогон: unit + integration + golangci-lint + govulncheck — все зелёные.
- [x] **G4** — Нет X-Request-ID прогона в тестах → при рефакторинге recover.go регрессия не заметна. Fix: integration-тест который триггерит panic в NATS handler и проверяет что X-Request-Id из header попал в лог. **2026-04-25:** инфра integration-тестов из G3 переиспользуется. Два уровня покрытия: (1) узкий тест в `internal/middleware/recover_test.go` (build tag `integration`) — embedded NATS, прямой `RequestMsgWithContext` с заголовком `X-Request-Id: abc-123` к Recover-обёрнутому panic-handler'у, проверяет: Status header = 500, body = `{"error":"internal server error"}`, log содержит `"req":"abc-123"`, `"panic":"boom"`, `"subject":"test.panic"`. Также `TestIntegration_RecoverPanicWithoutReply` — Pub/Sub паника без Reply, middleware только логирует, не пытается ответить. (2) широкий тест `TestIntegration_PanicPropagatesRequestID` в `gateway_integration_test.go` — полный production-flow: HTTP-клиент → Gateway генерирует `X-Request-Id` → проксирует в NATS → handler паникует → Recover отвечает 500 → Gateway возвращает HTTP 500 с тем же `X-Request-Id`. Проверяет, что request id из HTTP-ответа найден в общем log-буфере (Gateway и Recover пишут в один writer). Технические детали: thread-safe `safeBuf` (sync.Mutex + bytes.Buffer) для перехвата логов из NATS-горутин под `-race`; синхронизация через `done := make(chan struct{})` после возврата из Recover-обёртки в no-reply кейсе — гарантирует, что defer recover() успел отработать до чтения log-буфера. Финальный прогон: `go test -race ./...`, `go test -race -tags=integration ./...`, golangci-lint, govulncheck — все зелёные.
- [x] **G5** — Нет метрик. zerolog-логи в stderr — достаточно для current стадии, но prometheus-эндпоинт на Gateway (request rate, WS connection count, NATS request latency) значительно упростит инцидент-расследование. Fix: добавить `expvar` или `prometheus/client_golang`, выставить на 127.0.0.1:9100. **2026-04-25:** добавлен пакет `internal/platform/metrics/` на основе `prometheus/client_golang` (выбран вместо `expvar` — стандарт, гистограммы с метками, бесплатные `go_*`/`process_*` от рантайма). Эндпоинт `/metrics` запускается из `cmd/gateway/main.go` в отдельной горутине, bind по умолчанию `127.0.0.1:8081` (через `GATEWAY_METRICS_ADDR`). Порт изменён с аудит-формулировки `9100` (стандарт node_exporter, потенциальный конфликт) на `8081` — Gateway слушает 8080, +1 логически связан, доступ через тот же SSH-tunnel что и Nomad UI. Сбой metrics-сервера — log-only, не валит gateway. Метрики: `gateway_http_requests_total{service,method,status}`, `gateway_http_request_duration_seconds{service,method}` (Histogram с дефолтными bucket'ами 5ms…10s), `gateway_ws_connections_active` (Gauge, инкремент в `wsConnGuard`, декремент в release), `gateway_rate_limit_rejected_total{kind=general|auth}` (в `middlewareRateLimit`), `nats_request_duration_seconds{service}`, `nats_request_attempts_total{service,outcome=ok|no_responders|timeout|canceled|error}` (в retry-цикле `handleHTTP`). Метки `service`/`method` берутся ИЗ URL после маршрутизации — для невалидных путей метрика не пишется, кардинальность ограничена реальным набором бэкендов. Размещение в платформенном пакете (Q2 вариант 2): nc-клиент пока не использует, но при необходимости (например, метрики reconnect) подключается одной строкой. Тесты (Q6 вариант 2 — integration-only): `TestIntegration_MetricsCounterIncrements` поднимает Gateway + embedded NATS + httptest для `/metrics`, делает POST `/v1/echo/ping`, проверяет (а) delta счётчика через `prometheus/client_golang/testutil.ToFloat64`, (б) присутствие 4 expected-строк в выводе `/metrics` (counter, http duration, nats duration, nats attempts). `TestIntegration_MetricsRateLimitRejected` — отдельный тест с `Burst=1, Rate=0.1`: 2 GET-а подряд, второй отклонён → счётчик `general` инкрементировался. Инкрементальная реализация (Q8 вариант 1): сначала Step A (пакет + endpoint + один counter + integration-тест) → проверили wire-path, потом Step B (остальные 5 метрик одним заходом). CLAUDE.md раздел Metrics документирует endpoint, метрики и доступ. Финальный прогон: unit + integration + golangci-lint + govulncheck — все зелёные.
- [x] **G6** — Нет release notes / CHANGELOG. `build-N` pre-release'ы наполняются автоматом, но стабильные `v*` не имеют описания изменений. **2026-04-25:** на момент закрытия `release.yml` уже имел `generate_release_notes: true` для тегов `v*` (формулировка аудита устарела — этот флаг был добавлен раньше, до точечных правок 2026-04-25, см. `git log .github/workflows/release.yml`). Текущие изменения: (1) `ci.yml` — `generate_release_notes: false → true` для build-N pre-release'ов: pre-release'ы теперь содержат список merged PR с прошлого билда, не только tar.gz-файлы; полезно при rollback'ах для понимания «что было между build-N и build-N+1». (2) `.github/release.yml` — конфиг категоризации auto-notes: `Breaking changes` (label `breaking`), `Features` (`enhancement`, `feature`), `Bug fixes` (`bug`, `fix`), `Other` (`*` catch-all). Срабатывает и для `v*`-тегов, и для build-N pre-release'ов через тот же `generate_release_notes`. Manual `CHANGELOG.md` осознанно не вводился (Q1 вариант 5 принят) — для проекта без отдельных release-менеджеров friction обновлять файл перед каждым merge превышает ценность; auto-notes покрывают потребность через PR-метаданные. Лейбл `breaking` нужно создать в GitHub UI или через `gh label create breaking --color FF0000` — без него категория Breaking changes останется пустой; остальные лейблы (`bug`, `enhancement`, `documentation`) GitHub предоставляет автоматически. YAML валидирован `ruby -ryaml`.
- [x] **G7** — Нет dependabot/renovate конфига. NATS SDK, zerolog, gorilla/websocket будут отставать. Fix: добавить `.github/dependabot.yml` с weekly check. **2026-04-25:** создан `.github/dependabot.yml` с двумя экосистемами: `gomod` (директория `/`, weekly) и `github-actions` (директория `/`, weekly). Группировка minor+patch в один PR на экосистему через `groups.*.update-types: [minor, patch]` — major-апдейты остаются отдельными PR (требуют ручного ревью). Лимит открытых PR оставлен дефолтным (5). Renovate осознанно не выбран — Dependabot нативный, без внешнего GitHub App, конфиг в репо. Docker-экосистема не включена: compose-образы (NATS/Postgres) есть только в dev (`deployments/envs/dev/`), на прод не влияют.
- [x] **G8** — `README.md` рассинхронизирован с актуальной реализацией. Три места: (a) `:101-103` описывает CI/CD как «через `scp-action` бинарники копируются в `/usr/local/bin/`» — реально Nomad сам скачивает через `artifact{}`-блок из GitHub Releases, никакого scp на сервера нет; (b) `:192-193` структура каталогов ссылается на `platform.nomad` / `xservices.nomad`, которых не существует (актуальные — `gateway.nomad` + `xauth.nomad` + `xhttp.nomad` + `xws.nomad`); (c) `:115` Firewall-таблица указывает порт `80` для Gateway, реально слушает `8080`. Fix: переписать раздел CI/CD под актуальный flow (CI собирает → pre-release → Nomad artifact) и таблицу Firewall, синхронизировать структуру каталогов. Корневая причина — тот же класс дрейфа, что устранён в I-L9 удалением `docs/production-setup.md`: README дублирует разделы из CLAUDE.md/prod.md и расходится с ними. На этот раз минимальный фикс (точечная синхронизация трёх мест) приемлем — README — entry-point документации, удалять нельзя. **2026-04-25:** (a) раздел «Схема CI/CD» переписан в 4 пункта с явным указанием artifact-механизма (CI → pre-release `build-N` → `*-deploy.yml` → SSH + `nomad job run` → Nomad сам скачивает архив с проверкой SHA256), добавлена ссылка на `prod.md` для деталей; (b) Firewall-строка для Gateway: `80` → `8080`, дополнительно для `4646` уточнено «только loopback; внешний доступ через SSH-tunnel» (актуально после I-L6); (c) структура каталогов в разделе `deployments/infra/nomad/` переписана на актуальные 4 файла с пометкой `[DEMO]` для x-сервисов и `type=system` для gateway. Архитектурную задачу консолидации README ↔ prod.md ↔ CLAUDE.md осознанно не трогаем — entry-point должен оставаться самодостаточным; повторный G-пункт откроется при следующем дрейфе.

---

## Регрессии

- [x] **I-M9-regression** — `deployments/envs/prod/prod.md:415` — упоминается удалённый input `install_postgres` workflow setup.yml: «Поля: node_ip ..., platform_domain, install_postgres». Сам input был удалён в I-M9 (2026-04-17), но ссылка в документации осталась. Оператор, следующий инструкции, попытается задать поле и упрётся в ошибку workflow. Low — устаревшая строка в инструкции, не блокирует deploy. **2026-04-25:** scope расширен от audit-формулировки. Реальный список полей setup.yml: `node_ip` (required), `platform_domain` (required), `host_fingerprint` (optional — добавлен в I-H5, тоже не упоминался в prod.md). Заменил «`install_postgres`» на «`host_fingerprint` (опционально — SHA256-fingerprint host key, см. I-H5; пусто = TOFU)» — теперь оператор видит все три поля корректно. Без этого расширения возникла бы новая регрессия I-H5 в той же строке.
- [x] **I-C4-regression** — `deployments/envs/prod/prod.md:218-225` — шаблон нового сервиса в разделе «Добавить новый сервис» использует `checksum = var.CHECKSUM` как direct attribute блока `artifact`, минуя обязательный `options { ... }`-обёртку. Это та же ошибка синтаксиса, что закрыта в I-C4 для всех 4 реальных `.nomad`-файлов 2026-04-18. Контрибьютор, скопировавший шаблон, упрётся в `nomad job validate` при попытке задеплоить новый сервис. Medium — блокирует онбординг новых сервисов, не блокирует существующий deploy. **2026-04-25:** `checksum` обёрнут в `options { ... }` в шаблоне; добавлен inline-комментарий с ссылкой на I-C4 — будущий контрибьютор поймёт, почему обёртка обязательна, и не пойдёт «упрощать» обратно в `checksum = var.CHECKSUM`. Сверка с реальными `.nomad`-файлами (gateway/xauth/xhttp/xws) подтверждает идентичность синтаксиса.
- [x] **I-L4-regression** — `.github/workflows/release.yml:26` — `for svc in $(ls cmd/)` использует тот же паттерн, который был исправлен в `ci.yml` через `shopt -s nullglob; for dir in cmd/*/` (I-L4, 2026-04-18). При появлении файла в `cmd/` (`README`, `.gitkeep`, `.DS_Store`) versioned-релиз по тегу `v*` упадёт. Параллельный код в `release.yml` фикс не получил. Low — versioned-релизы сейчас не основной flow (auto-deploy идёт через `ci.yml` → `build-N`), но на ручной `v*`-релиз контрибьютор наткнётся. **2026-04-25:** применена идентичная правка из `ci.yml` — `shopt -s nullglob; for dir in cmd/*/; do svc="${dir%/}"; svc="${svc#cmd/}"; ...`. В шаге Build binaries добавлен 3-строчный комментарий со ссылкой на этот пункт, чтобы будущий контрибьютор не вернул `ls cmd/` обратно. YAML-валидность подтверждена `ruby -ryaml`, bash-синтаксис — `bash -n`. Audit-вариант через `find cmd -maxdepth 1 -type d` отвергнут по тем же причинам, что в I-L4 (зависимость от GNU `-printf`, лишний внешний бинарь для one-liner-а на bash glob).

---

## Волна 2026-04-26

**Источник:** архивировано из `STATUS.md` 2026-04-27.
**Охват:** Platform (P-M10, P-L6..L11), CI/CD+Infra (I-H7, I-M11, I-L10..L13), Demo (D-L4), Frontend (F-L1, F-L2), Global (G9). Этапы расширения scope: (1) основной проход — platform+infra+demo; (2) prod.md (I-L13), gateway_integration_test.go (P-L11); (3) xauth/xhttp/xws config-файлы (без новых находок); (4) xfrontend/ полностью (F-L1, F-L2).

---

### Platform

#### Medium

- [x] **P-M10** — `internal/services/gateway/handlers.go:236-237` (+ метки в `internal/platform/metrics/metrics.go:32,43,73,83`) — cardinality bomb на Prometheus-метрики. Метки `service`/`method` извлекаются из URL после `validSubjectToken`-валидации (`^[A-Za-z0-9_-]+$`), но это разрешает любую пару tokens, не только реальные backend-сервисы. Атакующий, посылающий 10⁴–10⁶ запросов на уникальные `/v1/{rand}/{rand}`, создаёт столько же label combinations в `gateway_http_requests_total` / `gateway_http_request_duration_seconds` / `nats_request_duration_seconds` / `nats_request_attempts_total`. NATS возвращает `ErrNoResponders` → 503, но `defer` уже инкрементировал метрику. Эффект: рост памяти client_golang (≈100 байт на timeseries × N), раздувание `/metrics`-выдачи (Prometheus federation захлёбывается), persistent ingestion bloat в downstream TSDB. (закрыт 2026-04-26: вариант (а), грануляция 1.A — обе метки сразу. В `handleHTTP` добавлен флаг `metricUnknown`, выставляется `true` при финальном `nats.ErrNoResponders` после исчерпания retry-цикла; defer для HTTP-метрик и блок NATS-метрик читают флаг и подставляют `service="unknown",method="unknown"` (для NATS — только service). Регрессивный риск «легитимный 503 на rolling deploy → unknown» принят: retry поглощает короткие окна, до метрики ErrNoResponders доходит только реально мёртвый subject. Doc-комментарии всех четырёх метрик в `metrics.go` обновлены. Добавлен `TestIntegration_MetricsUnknownService` в `gateway_integration_test.go`. `go build`, `go vet`, `go test -race`, `go test -race -tags=integration` — все зелёные.)

#### Low

- [x] **P-L6** — `internal/services/gateway/handlers.go:122-135` — `handleHealth` принимает любой HTTP-метод. POST/PUT/DELETE на `/health` отвечают 200. Не security-issue, но не идиоматично. (закрыт 2026-04-26: проверка добавлена в начало `handleHealth`; не GET/HEAD → 405.)
- [x] **P-L7** — `internal/services/gateway/handlers.go:168-202` — путь `/v1/{service}` (без method-сегмента) → `strings.Join([], ".")` = `""` → subject `api.v1.{service}.` (trailing dot — невалидный NATS subject). ErrNoResponders → 503, confusing лог. (закрыт 2026-04-26: проверка `len(parts) < 3` после WS-ветки в `route`; `/v1/{service}` отвечает 400 «method required» без обращения к NATS.)
- [x] **P-L8** — `internal/services/gateway/config.go:170-179` — `RateLimitConfig` не валидирует значения. `GATEWAY_RATE_LIMIT_MAX_IPS=-1` → eviction never triggers → unbounded growth. `GATEWAY_RATE_LIMIT=0`/`GATEWAY_RATE_BURST=0` → весь трафик отклоняется. `GATEWAY_MAX_WS_CONNS=-1` → все WS отклоняются. Silent degradation без логов. (закрыт 2026-04-26: добавлена `validateRateLimit` в `config.go`, fail-fast: `Rate`/`Burst`/`MaxIPs`/`MaxWSConns` ≤0 возвращают error; `AuthRate`/`AuthBurst` проверяются только при непустом `AuthPathPrefix`.)
- [x] **P-L9** — `internal/middleware/recover.go:30` — `Bytes("stack", debug.Stack())` zerolog сериализует как base64 — нечитаемо без декодирования. (закрыт 2026-04-26: `Bytes` → `Str(string(debug.Stack()))` — stack trace теперь JSON-escaped, читабелен через `jq -r '.stack'`.)
- [x] **P-L10** — `internal/services/gateway/handlers.go:266-268` — `r.Header.Get("Cookie")` возвращает только первое значение. RFC 6265 допускает несколько `Cookie:`-заголовков. При наличии 2+ теряются последующие. Аналогично в WS-ветке `handlers.go:539-541`. (закрыт 2026-04-26: оба места переведены на `strings.Join(r.Header.Values("Cookie"), "; ")`.)
- [x] **P-L11** — `internal/services/gateway/gateway_integration_test.go:343-376` — `TestIntegration_HealthEndpoint` проверяет только 200-ветку. Doc-комментарий обещает и 503-кейс при разрыве NATS, но он не реализован. Регрессия в `handleHealth` тестом не ловится. (закрыт 2026-04-26: добавлен `conn.Close()` + polling до 1s по `conn.IsConnected()` + GET `/health` → проверка StatusCode=503 + точное совпадение body `{"status":"error","nats":"disconnected"}`. `go test -race -tags=integration` зелёный.)

---

### CI/CD + Infra

#### High

- [x] **I-H7** — `.github/actions/deploy-nomad/action.yml:44-49` — SSH-команда использует `-o StrictHostKeyChecking=no`. Запускается на каждый push в main. Атакующий при MITM перехватывает: `NOMAD_TOKEN`, все `NOMAD_VAR_*`-секреты (`NATS_PASSWORD`, `AUTH_ACCESS_SECRET`, `AUTH_REFRESH_SECRET`, `DATABASE_URL`, `ALLOWED_HOSTS`, `GATEWAY_TRUSTED_PROXY`), deploy-команду. Симметрия с I-H5 диктует одинаковый уровень защиты. (закрыт 2026-04-26: в `action.yml` после `dig` — ssh-keyscan по всем хостам в `mktemp`-файл; при заполненном `HOST_FINGERPRINTS` — multi-host сверка через `ssh-keygen -lf` + grep; multi-line/comma-separated формат + `tr -d '\r'`; нерезолвящаяся нода исключается из деплоя без fail; несовпадение fingerprint'а = fail run + печать полученных fps; пустой секрет → TOFU + WARN. SSH перешёл на `StrictHostKeyChecking=yes`. В `*-deploy.yml` секрет `HOST_FINGERPRINTS` добавлен в env-блок job.)

#### Medium

- [x] **I-M11** — `deployments/infra/nats/nats.conf:17` (+ HEREDOC `deployments/envs/prod/setup.sh:413`) — `http_port: 8222` слушает на `0.0.0.0:8222`. UFW не открывает 8222 наружу, но единственный слой защиты — firewall. При `ufw disable` или провайдере с собственным firewall — public leak `/varz`, `/connz`, `/jsz`, `/healthz`. Defense-in-depth требует loopback-bind как первичного слоя. (закрыт 2026-04-26: `nats.conf` и HEREDOC-копия в `setup.sh` переведены на `http: "127.0.0.1:8222"`; комментарий заменён на defense-in-depth пояснение; print_summary логирует SSH-tunnel-инструкцию.)

#### Low

- [x] **I-L10** — `deployments/envs/dev/start.sh:172` — `for svc in $(ls "$ROOT_DIR/cmd/")`. Третья регрессия класса I-L4. (закрыт 2026-04-26: `build_binaries` в start.sh переведён на `shopt -s nullglob; for dir in cmd/*/`, идентично с ci.yml/release.yml.)
- [x] **I-L11** — `deployments/envs/prod/setup.sh:154-155` — GPG-ключ HashiCorp скачивается без верификации fingerprint'а. При компрометации APT-mirror'а на ноду ставится backdoored `nomad`-binary. Тот же класс, что I-H4 (NATS тарбол). (закрыт 2026-04-26: после `wget` — сверка fingerprint'а через `gpg --show-keys --with-colons` + `awk -F: '/^fpr:/ {print $10; exit}'`; expected=`798AEC654E5C15428C8E42EEAA16FCBCA621E701`; несовпадение → `die`.)
- [x] **I-L12** — `deployments/envs/prod/setup.sh:517-518` — `ufw allow 4646/tcp` после того, как I-L6 перевёл Nomad HTTP API на loopback. Правило — dead code и риск: при откате `addresses{}` из `nomad.hcl` UFW сразу откроет 4646 наружу. (закрыт 2026-04-26: строка `ufw allow 4646/tcp` удалена; комментарий явно указывает: Nomad HTTP API на loopback, UFW-правило было бы dead-code.)
- [x] **I-L13** — `deployments/envs/prod/prod.md` рассинхронизирован в трёх точках: (a) «`for svc in $(ls cmd/)`» — паттерн закрыт в I-L4/I-L4-regression; (b) «nomad job stop platform / xservices» — Legacy-инструкция для миграции с до-2026-04-18 архитектуры; (c) firewall-таблица содержит 4646 TCP между нодами — после I-L6 не нужен. (закрыт 2026-04-26: (a) переведено на канонический `shopt -s nullglob; for dir in cmd/*/`; (b) блок обёрнут в blockquote-warning «Legacy»; (c) строка 4646 удалена из firewall-таблицы, добавлено примечание о loopback-listener'ах.)

---

### Demo-сервисы

#### Low

- [x] **D-L4** — `internal/services/xhttp/handlers.go:185-217` — `HandleUpdate` не валидирует пустой `req.Name`. По симметрии с D-L1 (`HandleCreate` уже закрыт). (закрыт 2026-04-26: проверка добавлена после `json.Unmarshal` в `HandleUpdate` симметрично с `HandleCreate`. `go build` / `go vet` / `go test -race` зелёные.)

---

### Frontend

#### Low

- [~] **F-L1** — `xfrontend/src/tabs/Auth.tsx:5-6` — `useState('admin')` / `useState('dev-password')` хардкодит дефолтные credentials. После `vite build` попадают в `dist/assets/index-*.js`. (wontfix 2026-04-26: xfrontend — demo для ручного теста, хардкод `admin`/`dev-password` нужен ровно для UX «нажал login и оно работает»; production-проект xfrontend полностью заменяет своим фронтом, accept-risk валиден.)
- [x] **F-L2** — `xfrontend/src/api.ts:12-41` — `apiCall` использует `fetch` без таймаута / `AbortController`. При зависшем backend `fetch` ждёт ≥30s — кнопка залипает в `disabled`, пользователь не понимает что произошло. (закрыт 2026-04-26: `apiCall` обёрнут в try/catch; `signal: AbortSignal.timeout(8000)` вставлен после spread'а `init`; `DOMException name === 'TimeoutError'` → `{ok: false, status: 0, error: 'request timeout'}`. `tsc --noEmit` чисто.)

---

### Глобальные рекомендации

- [x] **G9** — `README.md` рассинхронизирован после волны фиксов 2026-04-25: (a) дерево каталогов не упоминает `internal/platform/metrics/`; (b) нет раздела «Метрики / Наблюдаемость». (закрыт 2026-04-26: (1) в дереве каталогов между `nc/` и `logger/` добавлен `metrics/` с описанием; (2) после раздела «Распределение ресурсов» добавлен раздел «Метрики и наблюдаемость» — loopback-bind 127.0.0.1:8081, ключевые метрики, SSH-tunnel, Prometheus federation, ссылка на CLAUDE.md → Metrics.)

---

### Контекст ре-аудита 2026-04-26

**Severity rationale:**
- **P-M10 Medium** (не High) — per-timeseries ~100 байт; gigabyte-scale требует миллионов уникальных path-комбинаций, что отсекается rate-limiter'ом (per-IP burst=200, default rate=100 r/s). Реальная жертва — downstream Prometheus federation / TSDB ingestion bloat.
- **I-H7 High** — симметрично с I-H5: те же секреты в SSH-сессии, тот же MITM-вектор; ключевое отличие — запускается на каждый push в main, а не один раз на новую ноду.
- **I-M11 Medium** (не High) — единственный текущий слой защиты — UFW; не leak секретов сам по себе (только метаданные через `/varz`/`/connz`), но при `ufw disable` для отладки — public leak.

**Что НЕ нашлось:** Critical/High в Platform: 0 (после волн 2026-04-17 и 2026-04-25); Critical/High в Demo: 0; Critical в Infra: 0. xauth/xhttp/xws config-файлы прочитаны — без новых находок. Frontend: supply-chain attack surface минимален (нет dangerous innerHTML, нет eval); `dist/` и `node_modules/` не в git.
