# deployments/dev/nats.conf
#
# Однонодовый NATS для локальной разработки.
# Без кластеризации, без аутентификации.

port: 4222
http_port: 8222

jetstream {
  store_dir: "/tmp/nats-dev-jetstream"
  max_mem: 256M
  max_file: 1G
}
