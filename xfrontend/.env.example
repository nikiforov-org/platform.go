# По умолчанию фронт ходит относительными путями через Vite dev-proxy
# на http://localhost:8080. Раскомментируй и задай абсолютный URL, если
# хочешь обращаться к Gateway напрямую (потребует CORS на стороне Gateway).
# VITE_GATEWAY_URL=http://localhost:8080
